import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images-na.ssl-images-amazon.com", "compressed.photo.goodreads.com"],
  },
};

export default nextConfig;
